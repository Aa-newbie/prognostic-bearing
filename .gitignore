# ─── Dataset (ไม่ push ขึ้น git — ดาวน์โหลดเองตาม README) ───
IMS/
IMS.zip
data/

# ─── โปรแกรมแตกไฟล์ของบุคคลที่สาม ───
tools/

# ─── Training artifacts ───
# เก็บใน git เฉพาะสิ่งที่ทำให้คนอื่นทำงานต่อได้โดยไม่ต้องมีข้อมูลดิบ 2.5 GB:
#   comparison_results.json  -> regenerate รายงานได้
#   compare_log.txt          -> log การเทรนจริง
#   features_cache.npz       -> บทเรียน notebook 2-8 รันได้เลย (192 KB)
# ส่วนรูปกับ checkpoint สร้างใหม่ได้ และรูปที่รายงานใช้ถูก copy ไป docs/assets/ แล้ว
outputs/*.png
outputs/*.pt
outputs/*/

# ─── Python ───
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.ipynb_checkpoints/

# ─── Editor / OS ───
.vscode/
.idea/
Thumbs.db
desktop.ini
.DS_Store

# ─── Claude Code local settings ───
.claude/
