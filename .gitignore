# ─── Dataset (ไม่ push ขึ้น git — ดาวน์โหลดเองตาม README) ───
IMS/
IMS.zip
data/

# ─── โปรแกรมแตกไฟล์ของบุคคลที่สาม ───
tools/

# ─── Training artifacts ───
# เก็บเฉพาะผลที่ใช้ regenerate รายงานได้ (comparison_results.json, compare_log.txt)
# ส่วนรูปกับ checkpoint สร้างใหม่ได้ — รูปที่รายงานใช้ถูก copy ไป docs/assets/ แล้ว
outputs/*.png
outputs/*.pt
outputs/*.npz
outputs/*/

# ─── Python ───
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.ipynb_checkpoints/

# ─── Editor / OS ───
.vscode/
.idea/
Thumbs.db
desktop.ini
.DS_Store

# ─── Claude Code local settings ───
.claude/
